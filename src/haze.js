// src/haze.js — Slice 3 single-source haze.
// HAZE_HEX sampled from the existing DAY preset fog (daytime.js), not invented:
// fog color (clear) = far-ring tint family. Sky keeps its warm gradient
// (art direction); the ring bakes toward this hue so the crest line melts.
export const HAZE_HEX = 0xcfd8dc;
